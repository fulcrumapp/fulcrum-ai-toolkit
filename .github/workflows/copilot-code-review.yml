name: Copilot Code Review

on:
  pull_request:

jobs:
  copilot-code-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
